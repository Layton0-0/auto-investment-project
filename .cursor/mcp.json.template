{
  "mcpServers": {
    "excel-reader": {
      "type": "stdio",
      "command": "C:/Users/HNW/AppData/Local/Python/pythoncore-3.14-64/python.exe",
      "args": ["D:/works/pjt/auto-investment-project/mcp-servers/excel-reader/server.py"],
      "env": {
        "MCP_EXCEL_PROJECT_ROOT": "D:/works/pjt/auto-investment-project",
        "PYTHONUNBUFFERED": "1"
      }
    },
    "filesystem": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem", "D:/works/pjt/auto-investment-project"]
    },
    "github": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-github"],
      "env": { "GITHUB_PERSONAL_ACCESS_TOKEN": "${GITHUB_TOKEN}" }
    },
    "github-actions-trigger": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@nextdrive/github-action-trigger-mcp"],
      "env": { "GITHUB_PERSONAL_ACCESS_TOKEN": "${GITHUB_TOKEN}" }
    },
    "figma": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "figma-developer-mcp@latest"],
      "env": { "FIGMA_API_KEY": "${FIGMA_API_KEY}" }
    },
    "ssh-mcp-oracle-osaka-yoon": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "ssh-mcp", "--", "--host=YOUR_ORACLE_OSAKA_PUBLIC_IP", "--port=22", "--user=ubuntu", "--key=YOUR_SSH_KEY_PATH_OSAKA", "--timeout=60000", "--maxChars=none"]
    },
    "ssh-mcp-oracle-korea-jihee": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "ssh-mcp", "--", "--host=YOUR_ORACLE_KOREA_PUBLIC_IP", "--port=22", "--user=ubuntu", "--key=YOUR_SSH_KEY_PATH_KOREA", "--timeout=60000", "--maxChars=none"]
    },
    "ssh-mcp-oracle-mumbai-yoon": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "ssh-mcp", "--", "--host=YOUR_ORACLE_MUMBAI_PUBLIC_IP", "--port=22", "--user=ubuntu", "--key=YOUR_SSH_KEY_PATH_MUMBAI", "--timeout=60000", "--maxChars=none"]
    }
  }
}
