# Environment and secrets (템플릿만 커밋)
.env
.env.local
.env.*.local
!.env.example
# MCP 실제 설정 (템플릿만 커밋)
mcp.json
.cursor/mcp.json

smart-portfolio-pal/*

# 로컬 전용·레거시 경로 (원격에 포함하지 않음)
shrimp_data/
investment-front/

# Claude Code 로컬 설정 (공유하지 않음)
.claude/